<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/search"
        android:icon="@drawable/pokeapp_ic_search"
        app:showAsAction="collapseActionView|ifRoom"
        android:visible="true"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        tools:ignore="MenuTitle" />

    <item android:id="@+id/about"
        android:icon="@drawable/pokeapp_ic_info_help"
        app:showAsAction="collapseActionView|ifRoom"
        tools:ignore="MenuTitle" />

    <item android:id="@+id/languages"
        android:icon="@drawable/pokeapp_ic_language"
        app:showAsAction="collapseActionView|ifRoom"
        android:visible="false"
        tools:ignore="MenuTitle">
        <menu>
            <item
                android:id="@+id/action_language_en"
                android:title="English" />
            <item
                android:id="@+id/action_language_es"
                android:title="Spanish" />
        </menu>
    </item>
</menu>
